{
  "name": "lfnawa-days-app",
  "version": "1.0.0",
  "private": true,
  "description": "Lfnawa Days shell + life-tracking features (app/src/life). Lfenwa Trades (app/trades/) is pre-built and untouched — this build step never recompiles it.",
  "scripts": {
    "build": "node build.js",
    "serve": "node serve.js"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-icons": "^5.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.7"
  }
}
